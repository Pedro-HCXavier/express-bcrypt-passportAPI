const hrefInicial = document.getElementById('href-inicial').addEventListener('click', () => {
    window.location.assign('http://localhost:3000/')
})

const hrefLogin = document.getElementById('href-login').addEventListener('click', () => {
    window.location.assign('http://localhost:3000/login')
})